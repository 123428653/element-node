const config = {
  // 服务器端口
  port: 3000,
  database: {
    database: 'vpn_admin_log',
    username: 'root',
    password: '123456',
    port: '3306',
    host: 'localhost'
  }
};

module.exports = config
